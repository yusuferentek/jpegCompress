
@{
    ViewBag.Title = "Index";
}
<script>

</script>
<style>
    .icon {
        width: 100px;
        height: 100px;
        text-align: center;
        position: relative;
        display: inline-block;
        margin-top: 300px;
    }

        .icon .img-top {
            display: none;
            position: absolute;
            top: 0;
            left: 0;
            z-index: 99;
        }

        .icon:hover .img-top {
            display: inline;
        }
    .button {
        border: none;
        color: white;
        padding: 15px 32px;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 16px;
        margin: 4px 2px;
        cursor: pointer;
        background-color: #bdc3c7;
        color: black;
        box-shadow: 0 8px 16px 0 rgba(0,0,0,0.2), 0 6px 20px 0 rgba(0,0,0,0.19);
    }
        .button:hover {
            background-color: black;
            color:white;
        }
</style>


@using (Html.BeginForm("Index", "Home", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <div class="icon">
        <label for="file-input">
            <img src="~/img/uploadicon.png" />
            <img src="~/img/uploadicondark.png" class="img-top" />

        </label>
        <input id="file-input" type="file" style="opacity:0;" value="imageInput" name="file" /> <br />
        <button type="submit" >İşlemi Başlat</button>
    </div>
}
    <div style=" margin-bottom: 300px; color: #151515">
        <h2>Resminizi buraya yükleyiniz.</h2>
        <button class="button" value="indir">Resmi İndir</button>
    </div>
